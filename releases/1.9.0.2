- удалил tempPath из настроек и вместо него ввел cachaDir в аргументах командной строки и PORTS.txt;
- удалил параметр hideDNS2Socks;
- сделал запись временных файлов в каталог с кешем;
- в Android версии сделал, чтобы логи отображались автоматически с конца;
- в Android версии поправил баг с падением;
- в Android версии добавил уведомление пользователя для возможности работы приложения в фоне;
- в Android версии поправил неправильное завершение tun интерфейса;
- в Android версии добавил передачу кеш директории в ShadowSocksConsole;
- в Android версии поправил незавершение сервиса VPN, если пользователь ничего не запускал;
- в Android версии поменял иконку.